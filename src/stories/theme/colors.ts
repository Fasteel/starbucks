export type Colors = {
  primary: string;
  secondary: string;
  green: string;
  greenDarker: string;
};

const colors: Colors = {
  primary: "#ffffff",
  secondary: "#000000",
  green: "#008248",
  greenDarker: "#00653e",
};

export default colors;
