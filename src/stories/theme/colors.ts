export type Colors = {
  primary: string;
  secondary: string;
  green: string;
};

const colors: Colors = {
  primary: "#ffffff",
  secondary: "#000000",
  green: "#008248",
};

export default colors;
